{lib_dirs, ["deps"]}.

{erl_opts, [
  warn_format,
  warn_export_vars,
  warn_obsolete_guard,
  warn_bif_clash
]}.

{deps, [
]}.

{pre_hooks, [
  {compile, "escript compile.escript"}
]}.

{cover_enabled, true}.
